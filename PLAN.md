# WebRTC Screen Share — 开发计划

## 阶段一：核心稳定性（P0 — 必须）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 1 | **房间管理** | Socket.IO rooms 隔离会话，支持 `?room=xxx` 参数配对 | ✅ |
| 2 | **STUN/TURN 配置** | 已移除 STUN（LAN-only 设计），无需 TURN | ⛔ 已关闭 |
| 3 | **ICE 重启与重连** | 监听 `connectionstatechange` + 自动 restartIce | ⏳ |
| 4 | **错误处理** | Sender 端 `getDisplayMedia` 拒绝的 try/catch，信令异常处理，用户提示 | ⏳ |

## 阶段二：用户体验（P1 — 重要）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 5 | **连接状态指示** | 接收端显示状态灯（等待中/连接中/已连接），sender 端显示连接状态 | ⏳ |
| 6 | **系统音频支持** | 添加 `audio: true`，可选采集系统音频 | ❌ |
| 7 | **断开/停止共享通知** | 一方断开时另一方收到通知并重置 UI | ❌ |
| 8 | **自动重连** | 短暂断连后自动恢复，无需手动刷新 | ❌ |
| 9 | **清理 `public copy/`** | 删除过期备份目录 | ✅ |

## 阶段三：功能增强（P2 — 锦上添花）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 10 | **一播多（Broadcast）** | 一个 sender 同时推流给多个 receiver | ❌ |
| 11 | **带宽自适应** | 通过 `getStats` 动态调节码率 | ❌ |
| 12 | **发送端预览** | sender 页面显示自己的共享画面（pip 小窗） | ❌ |
| 13 | **录制功能** | 用 `MediaRecorder` 录制共享会话 | ❌ |
| 14 | **文字聊天** | 在信令通道基础上增加文本消息交换 | ❌ |

## 阶段四：工程化（P3 — 可选）

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 15 | **HTTPS 支持** | 生产环境自签名证书 | ✅ |
| 16 | **Docker 化** | Dockerfile + ECR 镜像 | ✅ |
| 17 | **前端打包** | Vite/webpack/TypeScript | ❌ |
| 18 | **单元测试** | vitest 覆盖信令、HTTP、WebRTCManager | ✅ |

## 新增工程化（超出原计划）

| 任务 | 说明 | 状态 |
|------|------|------|
| **AWS 部署** | EC2 + EIP + VPC + Terraform | ✅ |
| **CI/CD 流水线** | GitHub Actions: check → build-push(ECR) → deploy | ✅ |
| **ECR + IAM** | 仓库生命周期策略（keep 2），EC2 角色 ECR 拉取权限 | ✅ |
| **ESLint** | 代码风格检查 | ✅ |
| **server.js 重构** | 导出 `createServer` 支持测试 | ✅ |

## 当前阶段状态

- ✅ **阶段一**：1/4 完成（任务 2 已关闭）
- ⏳ **阶段二**：1/5 完成
- ❌ **阶段三**：未开始
- ✅ **阶段四**：3/4 完成

## 剩余技术债务

- `public copy/` 目录待删除（若还存在）
- 接收端断连后停留在最后一帧，需清理
- 页面刷新后房间/状态丢失（浏览器刷新需重建连接）

## 建议下一步

### 优先级一：错误处理（Phase 1 #4）
- `getDisplayMedia` 拒绝/中止 → 用户提示
- 信令连接失败 → 重试/提示
- 接收端 WebRTC 连接失败 → 重置并提示

### 优先级二：断开通知（Phase 2 #7）
- 一方断开时另一方收到通知
- 接收端显示"发送端已断开"
- 发送端显示"接收端已离开"

### 优先级三：ICE 重连（Phase 1 #3）
- `connectionstatechange === 'failed'` → `restartIce()` + 重新 offer
- 短暂断网后自动恢复
