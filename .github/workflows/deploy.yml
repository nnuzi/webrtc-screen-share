name: CI/CD

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE: wss
  APP_PORT: "1443"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm test
      - run: npm run lint
      - run: docker build .

  build:
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Package source
        run: |
          tar czf /tmp/wss-src.tar.gz \
            --exclude='./node_modules' \
            --exclude='./.git' \
            --exclude='./terraform' \
            --exclude='./.github' \
            --exclude='./.DS_Store' \
            --exclude='./dist' \
            --exclude='./build' \
            -C "${{ github.workspace }}" .
      - uses: actions/upload-artifact@v4
        with:
          name: wss-src
          path: /tmp/wss-src.tar.gz
          retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: wss-src
          path: /tmp

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s' '${{ secrets.SSH_PRIVATE_KEY }}' > ~/.ssh/ec2-key.pem
          chmod 600 ~/.ssh/ec2-key.pem

      - name: Deploy to EC2
        env:
          SSH_OPTS: "-i ~/.ssh/ec2-key.pem -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
        run: |
          TARGET="ubuntu@${{ secrets.EC2_HOST }}"
          scp $SSH_OPTS /tmp/wss-src.tar.gz $TARGET:/tmp/wss-src.tar.gz
          ssh $SSH_OPTS $TARGET 'bash -s' <<'DEPLOY'
            set -e
            sudo rm -rf /opt/wss && sudo mkdir -p /opt/wss
            sudo tar xzf /tmp/wss-src.tar.gz -C /opt/wss
            cd /opt/wss
            sudo docker rm -f wss 2>/dev/null || true
            sudo docker build -t wss .
            sudo docker run -d --restart unless-stopped \
              -p 1443:1443 -e PORT=1443 \
              --name wss wss
            sudo docker rm -f cloudflared-tunnel 2>/dev/null || true
            sudo docker run -d --restart unless-stopped --network host \
              --name cloudflared-tunnel \
              cloudflare/cloudflared:latest tunnel --no-autoupdate \
              --url https://localhost:1443 --no-tls-verify
          DEPLOY

      - name: Get tunnel URL
        env:
          SSH_OPTS: "-i ~/.ssh/ec2-key.pem -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o LogLevel=ERROR"
        run: |
          sleep 10
          URL=$(ssh $SSH_OPTS ubuntu@${{ secrets.EC2_HOST }} \
            'sudo docker logs cloudflared-tunnel 2>&1' \
            | grep -oE 'https://[a-z0-9.-]+\.trycloudflare\.com' | head -1 || true)
          echo "### Deployment Info" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "**Tunnel URL:** $URL" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- Sender:  $URL/sender.html" >> "$GITHUB_STEP_SUMMARY"
          echo "- Receiver: $URL/receiver.html?room=YOUR_ROOM" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "Tunnel URL: $URL"
